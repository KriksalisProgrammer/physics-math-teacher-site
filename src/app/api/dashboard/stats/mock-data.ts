// Mock data generator for dashboard stats
// Used for static rendering fallback

export const getMockStats = () => {
  return {
    totalLessons: 12,
    completedLessons: 8,
    upcomingLessons: 3,
    activeSessions: 1
  };
};
